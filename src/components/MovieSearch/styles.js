import styled from 'styled-components';

const MovieSearchStyles = styled.div`
    text-align: center;
    padding: 50px;
`;
export const MovieContainer = styled.div`
    backgorund-color: lighgray;
`;

export default MovieSearchStyles;